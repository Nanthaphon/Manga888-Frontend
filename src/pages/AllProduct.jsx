import React from "react";
import CardAdmin from "./CardAdmin";

export default function AllProduct() {
  return (
    <div>
      <h1>fuck</h1>
      <CardAdmin
        price={el.price}
        book={el.name}
        imageUrl={el.image}
        id={el.id}
      />
    </div>
  );
}
